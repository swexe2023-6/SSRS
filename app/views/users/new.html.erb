<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>新規登録ページ</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
  <!-- その他のヘッダー要素（必要に応じて） -->
</head>
<body>
  <div class="container">
    <div class="row justify-content-center">
      <div class="col-md-6">
        <div class="card mt-5">
          <div class="card-header">
            新規登録
          </div>
          <div class="card-body">
            <%= form_tag({ action: :create }, class: 'form-group') do %>
              <div class="mb-3">
                <%= label_tag :email, 'メールアドレス', class: 'form-label' %>
                <%= text_field_tag :email, nil, class: 'form-control' %>
              </div>
              <div class="mb-3">
                <%= label_tag :password, 'パスワード', class: 'form-label' %>
                <%= password_field_tag :password, nil, class: 'form-control' %>
              </div>
              <div class="mb-3">
                <%= label_tag :password_confirmation, 'パスワード(確認)', class: 'form-label' %>
                <%= password_field_tag :password_confirmation, nil, class: 'form-control' %>
              </div>
              <div class="d-grid gap-2">
                <%= submit_tag '新規登録', class: 'btn btn-primary btn-lg' %>
              </div>
            <% end %>
          </div>
        </div>
      </div>
    </div>

    <h2 class="mt-5 text-center">ユーザー一覧</h2>
    <div class="table-responsive">
      <table class="table table-striped">
        <thead>
          <tr>
            <th>ユーザー名</th>
            <th>操作</th>
          </tr>
        </thead>
        <tbody>
          <% @users.each do |user| %>
            <tr>
              <td><%= user.email %></td>
              <td>
                <%= link_to '削除', "/users/#{user.id}", method: :delete, class: 'btn btn-danger btn-sm', data: { confirm: '本当に削除しますか？' } %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>

    <p class="text-center mt-3"><%= link_to '戻る', shangri_la_login_form_path, class: 'btn btn-secondary' %></p>
  </div>
</body>
</html>
